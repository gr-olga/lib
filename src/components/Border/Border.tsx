import {ReactNode} from "react";
import styles from './border.module.scss'

export const Border =(children:any)=>{
    return(
      <div className={styles.mainBorderContainer}>
hello
      </div>
    )
}
